import { useState, useEffect } from "react";
import { firebase } from "../firebase/app";

const blogCreated = () => {
  const [created, setCreated] = useState(false);

  useEffect(() => {
    // If crewated succesfully change state of created to true
    
  }, []);

  return { loginStatus };
};

export default useLogin;
